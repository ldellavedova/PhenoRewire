name: phenorewire
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  # Runtime dependencies live in pyproject.toml. Installing the package itself
  # keeps this file from drifting out of sync with them.
  - pip:
      - -e .
