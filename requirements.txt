# Runtime dependencies are declared in pyproject.toml; this file installs them
# via the package itself so the two can never drift apart.
#
#   pip install -r requirements.txt
#
# Optional extras:
#   pip install "phenorewire[viz]"        # --plot-networks, standalone 2D figure script
#   pip install "phenorewire[dev]"        # test suite and linter
-e .
